[project]
name = "orchestrator-webapp"
version = "0.1.0"
description = "Localhost dashboard for the provLedger orchestrator SQLite DB"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "jinja2>=3.1",
]

